{
  "name": "te-web",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --mode dev",
    "build:test": "vue-tsc -b && vite build --mode test",
    "build": "vue-tsc -b && vite build --mode prod",
    "preview": "vite preview"
  },
  "dependencies": {
    "naive-ui": "^2.40.1",
    "vfonts": "^0.0.3",
    "vxe-table": "^4.6.21"
  }
}
